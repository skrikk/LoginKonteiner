apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-lugeja
  namespace: arendus
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]   # ainult lugemine